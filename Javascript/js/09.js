// Booleanos

const boolean1 = true;
const boolean2 = false;

console.log(boolean1);
console.log(boolean2);

/*Los booleanos se usan por ejemplo cuando se tiene que autenticar un usuario ya que
existen solamente dos opciones el usuario SI está autenticado o NO está autenticado y de esto
depende si puede o no hacer ciertas funciones dentro de la página web*/